# 2026-09-05 同步核查与批量交付差距

## 同步事实

- 核查开始时本地 HEAD 与 origin/main 同为 d625c65，领先/落后均为 0。
- 本地只有未跟踪的 .claude/ 与 artifacts/，未发现未提交业务代码；这两个目录保留不动。
- PR #49（8ab8974）是独立 UI 草稿，涉及六个文件，尚未合并。PR #8 是 iOS 草稿，不能算可交付 iOS 产品。
- 本轮未重新核验生产服务器版本；此前发布日志记录为 2237311。源码同步不代表线上已更新。

## 本轮发现与修复

1. 护士完整档案页缺少 main 容器，全局侧栏让位规则未命中，造成截图中左侧内容遮挡。接入统一 main 容器。
2. 未关联患者时组件一直显示加载中。增加已加载但无档案的明确提示，成功重试后清除旧错误。
3. 病历允许手术日期早于出生日期。增加服务端验证与回归测试。
4. 依赖扫描发现 fast-uri/mysql2 间接依赖公告；采用同主版本修补覆盖，不降级 Prisma。

核查范围：同步状态、患者档案与页面骨架、现有测试、依赖扫描、网关权限、实时广播与备份实现。
这不是所有代码均无缺陷的证明，也不等于临床或量产验收。

## 结果导向的交付门槛

| 顺序 | 当前证据/缺口 | 下一项交付物 | 通过标准 |
| --- | --- | --- | --- |
| P0 | gateway-auth.ts 使用全局 GATEWAY_API_TOKEN；server-access.ts 对有效网关令牌直接放行患者访问 | 每设备凭据、患者授权、吊销与轮换、扫码配置 | A 设备不能向 B 患者写入；撤销立即生效；普通用户无需手填共享密钥 |
| P0 | 历史真机日志有数分钟延迟与积压，后续修补缺少完整长测报告 | 双 BT50 长测工具与可导出性能报告 | 30 分钟及 2 小时两档；正常网络最新帧 P95 <=2秒；离线重传不阻塞实时、不重不漏；断开显示离线 |
| P0 | ROM 与冲击规则有软件测试，量角器对照和误报数据不足 | 固定佩戴流程、参考角度测试集、误报复核集、算法版本记录 | 预先定义精度/重复性指标后盲测；正常屈膝、走动、取下设备不能无解释升级为患者危险 |
| P0 | 生产只有单护士回填验证，完整双护士流程尚无集成报告 | 两护士两患者隔离测试及并发抢码测试 | 名字修改跨端一致；抢占失败；释放后旧护士无权访问；新护士接管成功；审计完整 |
| P1 | sensor-live-broker.ts 为进程内广播，不能跨实例共享；未见目标并发容量报告 | 明确首批并发数量，压测与共享事件通道 | 按实际采样率持续压测；数据库/延迟/错误率在约定预算内；扩实例后不丢通知 |
| P1 | backup.sh 仅同机备份并保留14天 | 异地加密备份、恢复演习、故障通知、发布回滚 | 空白服务器可恢复；核对数据数量与权限；明确可接受数据损失和恢复时间 |
| P1 | 护士管理依赖管理员配置，设备批量发放流程不足 | 护士入职/停用、设备序列号台账、出厂绑定、固件/APK版本与售后诊断 | 新人员/设备可按书面步骤交付；离职撤权；故障包可定位具体版本和设备 |
| P1 | 护膝与传感器实物的电量、续航、佩戴一致性尚需实测 | 批次检验规范、充电/续航测试、佩戴说明、维护与替换规范 | 每台设备有检验记录；电量来源可追溯；断连/低电/异常可识别 |

建议先完成 P0 的设备授权与真实长测，再扩大试用人数。现阶段可定位为受控工程试用，不能仅凭页面和构建通过宣称可批量医疗使用。
若未来以医疗诊断或医疗器械用途销售，还需另行确认产品定位及适用要求，本报告不作法规符合性判定。
